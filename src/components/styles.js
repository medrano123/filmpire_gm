import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    root: {
        display: 'flex',
        height: '100%',
    },
    content: {
        flexGrowl: '1',
        padding: '2em',
    },
    toolbar: {
        height: '70px',
    }
}));